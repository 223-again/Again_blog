# 博客 · Obsidian 同步使用说明

这份文档说明如何用 Obsidian 写文章，并一键同步到博客。

## 一次性准备

1. **确认 Python 可用**。终端里跑 `python --version`，能看到版本号即可。
2. **在 Obsidian 里打开博客文件夹**。
   - 打开 Obsidian → 左下角「打开另一个库」→「以文件夹作为库打开」
   - 选择 `personal-blog\posts` 文件夹
   - 之后每篇文章就是这个文件夹里的一个 `.md` 文件

> 也可以把 `posts` 作为你已有库里的一个子文件夹，效果一样。

## 日常写作流程

1. **在 Obsidian 里新建 / 编辑 `.md` 文章**（格式见下）
2. **终端跑同步命令**（在仓库根目录 `personal-blog` 下）：

   ```
   python sync.py
   ```

3. **确认无误后提交**：

   ```
   git add -A
   git commit -m "更新文章"
   git push
   ```

推送后约 1 分钟，GitHub Pages 自动更新线上博客。

## 文章格式

每篇文章是 `posts/` 里的一个 `.md` 文件，开头用 `---` 包起来的一段是「元数据」，下面是正文。

```markdown
---
title: 文章标题
cat: tech
excerpt: 一两句话概括这篇文章
date: 2026-07-16
big: false
---

## 小标题

这是一段正文，可以有 `行内代码`。

​```
echo "这是代码块"
ls -la
​```

> 这是一段引用。
```

### 元数据字段

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | 建议填 | 文章标题，不填则用文件名 |
| `cat` | 是 | 分类：`essay`（随笔）/ `tech`（技术）/ `life`（生活） |
| `excerpt` | 建议填 | 首页卡片上显示的摘要 |
| `date` | 否 | 发布日期 `YYYY-MM-DD`，不填则用文件修改时间 |
| `big` | 否 | 填 `true` 时该文在首页显示为大卡片 |

### 正文语法

| 你写的 | 效果 |
|--------|------|
| `# 标题` ~ `###### 标题` | 标题 |
| `> 内容` | 引用块 |
| 三个反引号包住多行 | 代码块 |
| `` `文字` `` | 行内代码 |
| 其余文字，空行分段 | 普通段落 |

正文里的 `<` `>` `&` 会自动转义，直接写就行，不用手动处理。

## 常用命令

| 命令 | 作用 |
|------|------|
| `python sync.py` | 读取 `posts/*.md`，重建 `index.html` 里的文章 |
| `python sync.py --check` | 只检查有没有变化，不改文件 |
| `git diff` | 提交前查看这次改动 |

## 工作原理与注意事项

- **`posts/` 文件夹是唯一真相源**。同步是「全量重建」：`posts/` 里有哪些文章，博客就有哪些。
  - 在 Obsidian 删掉一个 `.md` = 博客删掉这篇文章
  - 改一个 `.md` = 改这篇文章
- **阅读时间自动计算**（按约 400 字/分钟），不用自己填。
- **文章按日期倒序排列**，最新的在最前面。
- `sync.py` **只改本地文件，不会自动提交**。所以 push 前你可以先 `git diff` 确认。
- 网页后台（连续点头像 5 次那个）依然可用，两套方式生成格式一致、互不冲突。

## 快速排查

- **提示「找不到文件夹 posts」**：确认在仓库根目录 `personal-blog` 下运行命令。
- **提示「未知分类」**：检查 `cat` 是否为 `essay` / `tech` / `life` 之一。
- **文章没出现**：可能正文为空，或文件名是 `README.md`（会被跳过）。
- **中文在终端显示乱码**：不影响文件内容，只是 Windows 终端的显示问题。
